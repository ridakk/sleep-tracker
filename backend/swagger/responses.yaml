---
components:
  responses:
    Success:
      description: successful operation
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/SuccessStatusCode'
    NotFound:
      description: The specified resource was not found
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    BadRequest:
      description: Bad Request
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    UnHealthy:
      description: Service unhealhty
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    BusyError:
      description: Too many request
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    InternalError:
      description: Service unhealhty
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
